import { Link } from "react-router-dom";

const Navigation = () => {
  return (
    <section>
      <nav>
        <Link to="/">Home</Link>
      </nav>
    </section>
  );
};

export default Navigation;
